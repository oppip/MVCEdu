using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace MVCEdu.Helpers
{
    public class AppSettings
    {
        //still not sure what this does, extra info on the db/transfer?
        //nesto so JWT tokenot, secret key stored in appsettings.json
        public string Secret { get; set; } 
    }
}
